{
  "name": "dm-pilot",
  "version": "0.1.0",
  "private": true,
  "description": "Local Instagram DM outreach tool: import LeadOS leads, write DMs with Claude, send assisted or paced-automatic.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/server.js --open"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "express": "^5.2.1",
    "playwright": "^1.63.0"
  }
}
